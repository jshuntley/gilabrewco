---
import BaseLayout from "./BaseLayout.astro";
import { Image } from "astro:assets";

const { content } = Astro.props;
const formattedDate = new Date(content.publishDate).toLocaleDateString(
  "en-us",
  {
    year: "numeric",
    month: "long",
    day: "numeric",
  }
);
---

<BaseLayout
  title={content.title}
  description={content.description}
  permalink={content.permalink}
  current="blog"
>
  <!-- Hero Section -->
  <div class="relative py-24 bg-desert-sand dark:bg-forest-green">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <div
          class="text-sm text-forest-green/60 dark:text-desert-sand/60 font-medium mb-4"
        >
          {formattedDate}
          {content.author && <span class="mx-2">·</span>}
          {content.author && <span>{content.author}</span>}
        </div>
        <h1 class="text-4xl md:text-5xl font-bold mb-6">{content.title}</h1>
        {
          content.description && (
            <p class="text-xl text-forest-green/80 dark:text-desert-sand/80 max-w-2xl mx-auto">
              {content.description}
            </p>
          )
        }
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <article>
      <!-- Featured Image -->
      {
        content.image && (
          <Image
            src={content.image}
            alt={content.title}
            width={1200}
            height={514}
            class="w-full aspect-[21/9] object-cover rounded-xl shadow-lg mb-12"
          />
        )
      }

      <!-- Content -->
      <div class="blog-content">
        <slot />
      </div>

      <!-- Tags -->
      {
        content.tags && content.tags.length > 0 && (
          <div class="mt-12 pt-6 border-t border-forest-green/10 dark:border-desert-sand/10">
            <div class="flex flex-wrap gap-2">
              {content.tags.map((tag: string) => (
                <span class="px-3 py-1 bg-forest-green/5 dark:bg-desert-sand/5 rounded-full text-sm text-forest-green/60 dark:text-desert-sand/60">
                  {tag}
                </span>
              ))}
            </div>
          </div>
        )
      }

      <!-- Share Links -->
      <div
        class="mt-12 pt-6 border-t border-forest-green/10 dark:border-desert-sand/10"
      >
        <div class="flex items-center justify-between">
          <div class="text-sm text-forest-green/60 dark:text-desert-sand/60">
            Share this post
          </div>
          <div class="flex gap-4">
            <a
              href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(content.title)}&url=${encodeURIComponent(content.permalink)}`}
              target="_blank"
              rel="noopener noreferrer"
              class="text-forest-green/60 dark:text-desert-sand/60 hover:text-canyon-red transition-colors duration-200"
              aria-label="Share on Twitter"
            >
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path
                  d="M6.29 18.251c7.547 0 11.675-6.253 11.675-11.675 0-.178 0-.355-.012-.53A8.348 8.348 0 0020 3.92a8.19 8.19 0 01-2.357.646 4.118 4.118 0 001.804-2.27 8.224 8.224 0 01-2.605.996 4.107 4.107 0 00-6.993 3.743 11.65 11.65 0 01-8.457-4.287 4.106 4.106 0 001.27 5.477A4.073 4.073 0 01.8 7.713v.052a4.105 4.105 0 003.292 4.022 4.095 4.095 0 01-1.853.07 4.108 4.108 0 003.834 2.85A8.233 8.233 0 010 16.407a11.616 11.616 0 006.29 1.84"
                ></path>
              </svg>
            </a>
            <a
              href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(content.permalink)}`}
              target="_blank"
              rel="noopener noreferrer"
              class="text-forest-green/60 dark:text-desert-sand/60 hover:text-canyon-red transition-colors duration-200"
              aria-label="Share on Facebook"
            >
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path
                  fill-rule="evenodd"
                  d="M20 10c0-5.523-4.477-10-10-10S0 4.477 0 10c0 4.991 3.657 9.128 8.438 9.878v-6.987h-2.54V10h2.54V7.797c0-2.506 1.492-3.89 3.777-3.89 1.094 0 2.238.195 2.238.195v2.46h-1.26c-1.243 0-1.63.771-1.63 1.562V10h2.773l-.443 2.89h-2.33v6.988C16.343 19.128 20 14.991 20 10z"
                  clip-rule="evenodd"></path>
              </svg>
            </a>
          </div>
        </div>
      </div>
    </article>

    <!-- Navigation -->
    <nav
      class="mt-12 pt-6 border-t border-forest-green/10 dark:border-desert-sand/10"
    >
      <div class="flex justify-between">
        <a
          href="/blog"
          class="inline-flex items-center text-forest-green/60 dark:text-desert-sand/60 hover:text-canyon-red transition-colors duration-200"
        >
          <svg
            class="w-5 h-5 mr-2"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M15 19l-7-7 7-7"></path>
          </svg>
          Back to Blog
        </a>
      </div>
    </nav>
  </div>
</BaseLayout>
